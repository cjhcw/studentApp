<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.Student.MainActivity"
    android:background="@drawable/picture" >
    <Button
        android:id="@+id/button1_first"
        android:background="@drawable/shape"
        android:textColor="@color/write"
        android:textSize="25sp"
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="20dp"
        android:text="@string/login_denglu" />
    <Button
        android:id="@+id/button2_second"
        android:textColor="@color/write"
        android:layout_width="120dp"
        android:layout_height="60dp"
        android:textSize="25sp"
        android:background="@drawable/shape"
        android:layout_alignParentRight="true"
        android:layout_alignBaseline="@id/button1_first"
		android:layout_marginBottom="20dp"
        android:layout_marginRight="20dp"
        android:text="@string/set_zhuce" />

</RelativeLayout>